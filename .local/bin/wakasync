#!/bin/sh

#
# Syncs wakatime offline activity
#

wcount() {
	wakatime-cli --offline-count
}

wsync() {
	wakatime-cli --sync-offline-activity "$1"
}

while [[ $(wcount) -gt 1 ]]; do # just for making sure
	wsync $(wcount)
done

